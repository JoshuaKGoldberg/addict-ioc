'use strict';

const should = require('should');

const container = require('./../../lib/container');

class TestType {}

describe('Dependency Injection Container Get Keys By Attributes Test', function describeCallback() {

  beforeEach(() => {
    container.clear();
  });

  it.skip('should get keys by single attribute', function testCallback() {
  });

  it.skip('should get keys by multiple attributes', function testCallback() {
  });

  it.skip('should not get keys without single attribute', function testCallback() {
  });

  it.skip('should not get keys without multiple attributes', function testCallback() {
  });
});
