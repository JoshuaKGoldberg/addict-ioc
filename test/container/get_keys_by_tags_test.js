'use strict';

const should = require('should');

const container = require('./../../lib/container');

class TestType {}

describe('Dependency Injection Container Get Keys By Tags Test', function describeCallback() {

  beforeEach(() => {
    container.clear();
  });

  it.skip('should get keys by single tag', function testCallback() {
  });

  it.skip('should get keys by multiple tags', function testCallback() {
  });

  it.skip('should not get keys without single tag', function testCallback() {
  });

  it.skip('should not get keys without multiple tags', function testCallback() {
  });
});
